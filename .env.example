# 数据库配置
DB_HOST=mysql
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_strong_password_here
DB_NAME=linglow_chat

# Session密钥（随机生成一个长字符串）
SESSION_SECRET=your_random_session_secret_here

# 服务器配置
NODE_ENV=production
PORT=8816

# Web Push配置（可选）
# 使用 node generate-vapid-keys.js 生成
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_MAILTO=mailto:your@email.com
